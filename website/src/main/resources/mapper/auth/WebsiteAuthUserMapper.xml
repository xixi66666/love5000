<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.website.auth.dao.WebsiteAuthUserDao">

    <resultMap id="AuthUserResultMap" type="com.example.common.auth.model.AuthUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="display_name" property="displayName"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="role" property="role"/>
        <result column="enabled" property="enabled"/>
    </resultMap>

    <select id="findByUsername" resultMap="AuthUserResultMap">
        select id, username, display_name, password_hash, role, enabled
        from auth_user
        where username = #{username}
    </select>

    <select id="findById" resultMap="AuthUserResultMap">
        select id, username, display_name, password_hash, role, enabled
        from auth_user
        where id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.common.auth.model.AuthUser"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into auth_user(username, password_hash, display_name, role)
        values (#{username}, #{passwordHash}, #{displayName}, #{role})
    </insert>
</mapper>
