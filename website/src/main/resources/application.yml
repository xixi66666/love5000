spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: kuosi
    url: jdbc:mysql://49.232.128.132:3306/xixi?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

  web:
    resources:
      static-locations:
        - classpath:/static/

server:
  servlet:
    session:
      cookie:
        http-only: true
        same-site: strict

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.example.website.blog.model,com.example.common.auth.model
  configuration:
    map-underscore-to-camel-case: true

python-a:
  auto-start:
    enabled: true
    work-dir: python-a
    command: python
    port: 5174
    health-path: /api/health
    startup-timeout-seconds: 20
    log-to-console: true

quant-a:
  auto-start:
    enabled: true
    work-dir: quant-a
    command: "C:\\Users\\xixixiaozi\\AppData\\Local\\Programs\\Python\\Python311\\python.exe"
    port: 5175
    health-path: /api/health
    startup-timeout-seconds: 30
    log-to-console: true

video:
  auto-start:
    enabled: true
    work-dir: video
    command: python
    port: 5176
    health-path: /api/health
    startup-timeout-seconds: 30
    log-to-console: true
